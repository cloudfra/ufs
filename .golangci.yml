version: "2"

linters:
  # Keep the default set (errcheck, govet, ineffassign, staticcheck, unused)
  # and layer a security-focused, curated set on top of it.
  default: standard
  enable:
    - gosec
    - gocritic
    - unconvert
    - unparam
    - prealloc
    - bodyclose
    - noctx
    - goconst
